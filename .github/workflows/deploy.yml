name: Deploy Staic website to AWS

on: 
  push:
    branches: 


jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      
      
    
